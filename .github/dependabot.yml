version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    # Wait 7 days after a release before proposing it (supply-chain caution; exceeds the 24h
    # minimumReleaseAge floor, so any version Dependabot proposes already satisfies the install guard).
    cooldown:
      default-days: 7
    groups:
      svelte:
        patterns:
          - '@sveltejs/*'
          - 'svelte'
          - 'svelte-*'
      testing:
        patterns:
          - 'vitest'
          - '@vitest/*'
          - '@playwright/*'
          - 'playwright'
          - 'vitest-*'
      linting:
        patterns:
          - 'eslint'
          - '@eslint/*'
          - 'eslint-*'
          - 'prettier'
          - 'prettier-*'
          - 'typescript-eslint'
      types:
        patterns:
          - '@types/*'
          - 'typescript'
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
