// Public content, but client-only: Ask spins up a Web Worker + fetches the corpus (browser-only) and
// runs embedding/search on-device. Never server-rendered or prerendered (mirrors /timeline).
export const ssr = false;
export const prerender = false;
